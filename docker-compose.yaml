services:
  server:
    build:
      context: ./server
    container_name: security-course-server
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      MONGO_URI: ${MONGO_URI}
      JWT_SECRET: ${JWT_SECRET}
      CLIENT_URL: "http://72.61.98.20"

  web:
    build:
      context: ./client
      args:
        VITE_API_URL: "http://72.61.98.20:5000/api"
    container_name: security-course-web
    ports:
      - "80:80"
    depends_on:
      - server